/* --------------------------------------------------------------------------
   NO-OP Seed

   Diese Datei bleibt absichtlich ohne echte Seed-Daten.
   Alle Seed-Daten liegen zentral in database/seeds/seed_initial_data.sql.

   Wichtig:
   Kein SELECT verwenden, weil der aktuelle Seeder Resultsets nicht puffert.
-------------------------------------------------------------------------- */
SET @vdbs_seed_noop = 1;
