-- Schema comments are now embedded directly in the CREATE TABLE / ALTER TABLE statements
-- of all migrations. This migration remains as a compatibility marker for the
-- historical migration number and intentionally performs no schema changes.
SELECT 1 AS `schema_comments_embedded`;
