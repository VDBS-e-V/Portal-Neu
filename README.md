# VDBS Portal (PHP MVC Skeleton)

## Voraussetzungen
- PHP 8.2+
- Composer
- (optional) XAMPP / Apache / Nginx

## Setup
1. `composer install`
2. `.env.example` nach `.env` kopieren und anpassen
3. Webserver-Docroot auf `public/` zeigen lassen

## Routen
- `/` (HTML)
- `/api/users` (JSON)